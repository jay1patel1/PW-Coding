let age = 65;
let siniorcitzenage = 60 // Sinior citizenage limit
let discount;

let willGetDiscount = (age > sinorcitzenage) ? "Rlease coupen code" : "Print your age is lesser than senior citizen age limit"